# ▶ 여기부터 — PIXEL TRADING FLOOR 설치 안내

AI 에이전트들이 시장을 분석하고 토론해 매매 판정을 내리는 픽셀아트 로컬 웹앱입니다.
**분석·시뮬레이션 도구이며 실제 주문은 이뤄지지 않습니다.**

---

## 1. 압축을 풉니다

아무 폴더에나 풀면 됩니다. 경로에 한글·공백이 있어도 동작합니다.

## 2. 그 폴더를 Claude Code로 엽니다

터미널에서:

```
cd 압축푼폴더/trading-floor
claude
```

또는 Claude Code 데스크톱 앱에서 해당 폴더를 여세요.

## 3. Claude Code에 이 문장을 그대로 붙여넣습니다

```
CLAUDE.md 읽고 이 프로젝트 실행해줘. 서버 띄우고 브라우저로 확인까지.
```

폴더에 `CLAUDE.md`가 들어 있어서, Claude Code가 구조·실행법·주의사항을 알아서 파악하고
서버를 띄워줍니다. 별도 설정은 필요 없습니다.

---

## 직접 실행하고 싶다면

```
node server/server.js
```

브라우저에서 **http://localhost:8000**

Windows는 `start-floor.cmd` 더블클릭으로도 됩니다.
8000번 포트가 이미 쓰이는 중이면 `PORT=8123 node server/server.js`.

---

## 준비물

| | 필요 여부 | 비고 |
|---|---|---|
| **Node.js 20 이상** | 필수 | https://nodejs.org — `node -v`로 확인 |
| npm 설치 | **불필요** | 외부 패키지를 하나도 안 씁니다 |
| API 키 | **불필요** | 전부 키 없는 공개 API |
| `claude` CLI | 실전 모드만 | 없으면 아래 데모 모드로 |

## 데모 모드 — CLI 없이 화면만 보고 싶을 때

**http://localhost:8000/?demo=1**

`claude` CLI 없이도 고정 목업 응답으로 에이전트 7명, 토론, 판정, 리포트 저장까지
전 과정이 그대로 돕니다. 처음이라면 여기부터 보세요.

---

## 사용법

1. 상단에 심볼 입력 — `BTC`, `ETH`, `TSLA`, `하이닉스`, `삼성전자`, `005930` …
2. 모드 선택
   - **알고리즘** — 애널리스트 4명 → BULL⇄BEAR 토론 4턴 → ACE 최종 판정
   - **⚡스캘핑 20x** — 단타 전용. 관망(PASS) 판정도 나옵니다
   - **⚔공격** — 관망 금지. 반드시 롱/숏 중 하나가 나옵니다 *(시연·영상용)*
3. **▶ ANALYZE**

결과는 `reports/` 폴더에 마크다운으로 쌓이고, 브라우저 `/reports`에서도 볼 수 있습니다.

### 한국 주식은 전광판이 함께 뜹니다

SK하이닉스·삼성전자는 USDT 무기한 선물이 여러 거래소에 상장돼 있어서, 화면 상단에
**KRX 현물 / 거래소별 선물 / 환율 / 선물↔KRX 괴리 / 해외 상장**을 한 번에 보여줍니다.
스캘핑 판정은 정규장 원화 차트가 아니라 실제로 체결되는 24시간 USDT 차트를 기준으로 냅니다.

### 슬래시 커맨드

Claude Code 안에서:

```
/floor 하이닉스 scalp
/floor BTC algo
/floor 삼성전자 공격
```

서버를 띄우지 않고 현재 세션에서 에이전트 전원을 연기합니다.

---

## ⚠ 읽고 쓰세요

- **AI 시뮬레이션이며 투자 조언이 아닙니다.** 판정을 그대로 따라 매매하지 마세요
- **거래소 연동·자동 주문 기능은 없습니다.** 넣지 마세요
- 20배 레버리지 판정이 나와도 **-5% 역행 한 번에 증거금 전액이 청산**됩니다
- **⚔공격 모드는 "우위가 없어도 한쪽을 고르게" 강제한 연출용 모드**입니다.
  확신도 50%대는 사실상 동전던지기라는 뜻이니, 방향만 보지 말고 확신도와
  무효화(손절) 레벨을 반드시 함께 보세요
