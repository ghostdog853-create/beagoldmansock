# WAVE FLOOR — Claude Code 안내

지표와 **글렌 닐리(Glenn Neely)의 NEoWave**를 기반으로 단기·중기·장기 세 시간축을
각각 분석하고, 그 셋이 같은 방향인지 판정한 뒤, 실행 계획으로 옮기는 픽셀아트 로컬 웹앱이다.

**파동 이론의 근거 문서는 `docs/파동분석-통합본.md`다.** 파동 관련 작업을 하기 전에
반드시 읽어라 — 되돌림 법칙 표, 구조기호 체계, 미구현 항목이 전부 거기 있다.
원자료 15종의 추출본은 `docs/source/`에 있다.

**분석·시뮬레이션 도구다. 거래소 주문 연동은 없고, 앞으로도 넣지 않는다.**

## 실행

```bash
node server/server.js          # http://localhost:8100
# 포트 충돌 시: PORT=8123 node server/server.js
# Windows: start-wave.cmd 더블클릭
```

의존성 설치 불필요 — `package.json`에 dependencies가 없고 Node 내장 모듈만 쓴다.

**전제조건**
- Node 20 이상 (global `fetch`, `AbortSignal.timeout`)
- 실전 모드는 `claude` CLI가 PATH에 있고 로그인돼 있어야 한다 (에이전트마다 `claude -p` 스폰)
- CLI 없이 화면만 보려면 데모 모드 `http://localhost:8100/?demo=1`

## 이 프로젝트의 핵심 원칙

### 1. 코드가 세고, LLM은 해석한다

**LLM에게 캔들을 던지고 "엘리어트 파동을 세어봐"라고 하면 안 된다.** 좌표 없는 환각이
나오고 매번 다르게 센다. 그래서 `indicators/`가 피벗 검출 → 파동 후보 생성 → 규칙
검증 → 피보나치 채점까지 전부 끝낸 뒤, **살아남은 후보 목록만** 프롬프트에 넣는다.

에이전트 프롬프트에는 이 규칙이 명시적으로 박혀 있다 (`agents.js` `GROUNDING_RULE`):

> 엘리어트 파동은 이미 코드가 규칙 검증까지 마친 후보 목록이 주어진다 —
> 네가 새로 세지 말고, 주어진 후보 중에서 고르고 그 이유를 대라.

이 원칙이 깨지면 프로젝트 전체가 무의미해진다. **새 지표를 추가할 때도 반드시
"코드가 계산 → 문장으로 렌더 → 프롬프트 주입" 순서를 지켜라.**

### 2. 분업 축은 "시간"이다

에이전트를 기법별(추세/모멘텀/패턴)로 나누지 않았다. 시간축별로 나눴다.
실전에서 방향을 결정하는 것은 개별 지표가 아니라 **"큰 시간축과 작은 시간축이
같은 쪽을 보는가"** 이기 때문이다. 장기 상승 + 단기 눌림 = 매수 기회.

### 3. 모든 판정에 무효화 레벨이 따라온다

파동 후보, 차트 패턴, 시간축 분석, 실행 계획 — 전부 "이 가격이 깨지면 이 판단은
죽는다"를 함께 낸다. 엘리어트의 절대 규칙이 깨지는 지점이 곧 손절선이므로
이게 자동으로 나온다. **무효화 레벨 없는 판정은 이 프로젝트에서 결함이다.**

## 구조

```
server/
  server.js          HTTP 라우팅 · SSE · 리포트 열람/ZIP (외부 의존성 0)
  engine.js          모드별 파이프라인 오케스트레이션 → 이벤트 방송 → 리포트 저장
  agents.js          역할별 프롬프트 빌더 · claude 스폰 · 데모 목업
  timeframes.js      멀티 타임프레임 캔들 수집 (바이낸스) · 시간축 정의
  tape.js            하단 티커
  indicators/
    pivot.js         ★ ZigZag 피벗 검출 — 아래 전부의 기반. 모노파동 = 피벗 구간
    scale.js         ★ 비례의 법칙 — 산술/로그 척도 이중 측정 · 척도 분기 검출
    neely-patterns.js ★ 닐리 원서 12장 전량 카탈로그 — 세력 순위 19종, 유사성·시간론·
                        예외의 법칙, 구조기호 흐름, 복잡성 등급, x파동 조합표, 고급 피보나치
    preconstruction.js ★ 논리적 사전 구성법칙 (닐리 3장) — 구조기호 확정 결정표
    neowave.js       ★ 닐리 되돌림 법칙 1~7 · 구조기호 · 방향성/비방향성 국면 (생성기)
    elliott.js       ★ 고전 3법칙 검증 + 피보나치 채점 (검증기)
    ma.js            SMA 5/20/60/120/200 · 정배열/역배열 · 골든/데드크로스
    oscillator.js    RSI · MACD · 스토캐스틱 · 다이버전스
    channel.js       선형회귀 채널 · 피벗 추세선 · 볼린저
    pattern.js       헤드앤숄더 · 이중천정/바닥 · 삼각수렴 · 깃발
    diagonal.js      ★ 다이아고날 3대 조건 스크리너 · 터미널 임펄스 감별
    corrective.js    지그재그(B파 등급제) · 플랫 8종 · 삼각형 · C파 확률 분포
                     ★ 복합조정 WXY/WXYXZ — x파동 크기로 범주 1/2 판정
    harmonic.js      ★ 하모닉 9종 XABCD — 엄격 앵커링 필수
    volume.js        구간별 거래량 — 파동 카운트 확인용
    methods.js       ★ 실전 기법 (텔레그램 [T1~T5] + 총정리.pdf [S]) — 역피보 반등 ·
                        엘리엇 앵커 채널 · MA99 · 거래량 사이클 · 피보 꿀통 ·
                        피보나치 수열 매물대 · EMA 대작전 · 피보 타임존
    arbitrate.js     ★ 중재 — 닐리 vs 실전 기법, 기저율 대비 초과 적중으로 비교
    confluence.js    ★ 근거 중첩 — 닐리를 베이스로 모든 레벨을 클러스터링
    index.js         시간축별 조립 + 프롬프트용 렌더링
public/              단일 페이지 프론트 (캔버스 스프라이트 + DOM, 빌드 도구 없음)
reports/             런마다 마크다운 리포트 + decisions.json 누적
```

**`pivot.js`가 심장이다.** 채널·패턴·파동·다이버전스가 전부 "의미 있는 고점·저점
목록"에서 출발한다. 여기가 부실하면 위에 얹는 게 전부 무너진다.

**파동은 두 층으로 돈다. 순서가 중요하다.**
1. `neowave.js` — 각 모노파동의 구조를 되돌림 비율만으로 판정한다. **생성기**
2. `elliott.js` — 고전 3법칙으로 규칙 위반 카운트를 거른다. **검증기**

고전 엘리어트만으로는 "이 카운트가 규칙을 어겼는가"만 물을 수 있다. 닐리는
"이 조각이 충격파인가 조정파인가"를 비율로 답한다. 후보를 만들어내는 쪽이 먼저다.

## 에이전트와 모드

| 에이전트 | 역할 | 받는 데이터 |
|---|---|---|
| DASH | 단기 분석 | 15m·1h 지표 묶음 |
| SWING | 중기 분석 | 4h·1d 지표 묶음 |
| TIDE | 장기 분석 | 1d·1w 지표 묶음 |
| PRISM | 시간축 정렬 판정 | 세 분석가 리포트 + 정렬표 |
| PILOT | 실행 설계 | 정렬 판정 + 모든 레벨 |

시간축 분석가 3인은 **병렬**(서로의 결과가 필요 없다), PRISM·PILOT은 **순차**다.

| 모드 | 시간축 | 용도 |
|---|---|---|
| `full` | 단기·중기·장기 | 전체 3축 정렬 판정 |
| `swing` | 중기·장기 | 단기 노이즈 제외한 스윙 관점 |
| `scalp` | 단기·중기 | 단타 관점 |

모드는 `engine.js`의 `MODES` 표 하나로 정의된다. **엔진 코드를 건드리지 말고
표만 바꿔서 새 모드를 추가하라.**

### PRISM의 정렬 판정 4단계

- `ALIGNED_UP` / `ALIGNED_DOWN` — 세 축 모두 같은 방향. 추세 추종이 유리
- `MIXED` — 큰 축들은 같은 방향인데 단기만 반대. **되돌림 = 진입 기회**
- `CONFLICT` — 장기와 중기가 정면으로 반대. **관망이 정답일 때가 많다**

MIXED와 CONFLICT를 혼동하면 안 된다. 이 구분이 이 프로젝트의 결론 그 자체다.

## 닐리 NEoWave 구현 상세

`neowave.js`. 전체 명세는 `docs/파동분석-통합본.md` 1장.

**되돌림 법칙** — `m2/m1` 비율이 법칙 번호(1~7)를, `m0/m1`이 조건(a~f)을 정한다.
법칙 4만 `m3/m2`로 범주(i~iii)가 한 단계 더 갈린다. 결과는 **구조기호**다
(`:5` `:3` `:F3` `:c3` `:L5` `:s5` `x:c3`).

**괄호가 곧 신뢰도다** — 맨몸(1.0) > `(소괄호)`(0.6) > `[대괄호]`(0.3).
별도 채점 로직 없이 이 가중치를 그대로 쓴다.

**중립성의 법칙** — 되돌림 비율이 판정 경계(38.2/61.8/100/161.8/261.8%)에 ±3%p 이내로
붙으면 양쪽 법칙을 **모두 후보로 남긴다**. 경계에서 하나만 고르면 그 뒤가 전부 틀어진다.

**방향성/비방향성 국면 판정이 정렬 판정(PRISM)의 1급 재료다.** 비방향성 국면에서
방향 베팅을 설계하는 것이 손실의 주된 원인이기 때문이다.

**포지션 지표 가장자리 필터** — `:c3`는 패턴의 시작점도 끝점도 될 수 없고 `x:c3`도
연속 파동의 양 끝에 올 수 없다(닐리 3장). 첫·마지막 모노파동에서 이 둘을 후보에서
뺀다. 단 **후보가 0이 되면 원래 집합을 되살린다** — 좁히는 게 목적이지 없애는 게 아니다.

> **한때 추정이었던 구간은 이제 확정이다.** 법칙 6·7의 조건 경계와 법칙 4·6·7의
> 기호집합은 원서 p.118 · p.159~164에서 확인해 채웠고 `estimated`/`partial` 플래그가
> 해제됐다. **다만 플래그 메커니즘 자체는 남겨두라.** 새로 복원 못 한 값이 생기면
> 반드시 다시 표시해야 한다 — 없는 근거를 만들어내지 않기 위한 장치다.

## 고전 엘리어트 검증 상세

절대 규칙 (깨지면 후보 폐기, `elliott.js`):

| 규칙 | 내용 |
|---|---|
| R1 | 2파는 1파 시작점을 넘어 되돌리지 못한다 |
| R2 | 3파는 1·3·5 중 최단일 수 없다 |
| R3 | 4파는 1파 가격 영역을 침범하지 못한다 |

가이드라인은 규칙이 아니라 **점수**다 — 2파 50/61.8/78.6%, 3파 161.8/261.8%,
4파 23.6/38.2%, 5파는 1파의 61.8/100/161.8%. 교대 지침(2파와 4파의 깊이 차이)은 가감점.

**완성도 가중치가 반드시 곱해진다.** 파동 2개짜리 후보가 4파까지 확인된 후보보다
높은 점수를 받으면 순위가 거꾸로 선다. 근거가 적을수록 확신도가 낮아야 한다.

## 작업 규칙

- **외부 의존성을 추가하지 않는다.** npm 패키지 0개가 이 프로젝트의 제약이자 이식성이다
- **데이터에 없는 수치를 지어내지 않는다.** 없으면 "데이터 없음"으로 표기
- **피벗은 항상 서로 다른 봉에 찍혀야 한다.** 변동폭 큰 캔들 하나에 고점·저점이
  동시에 잡히면 시간 순서가 무너져 엘리어트 카운트가 전부 깨진다.
  `pivot.js`에서 되돌림 검사를 극값 갱신보다 **먼저** 하는 이유가 이것이다
- 시간대별 ZigZag 임계값(`indicators/index.js` `PIVOT_K`)을 함부로 낮추지 마라.
  15분봉에서 피벗이 수백 개 잡히면 파동 카운트가 무의미해진다
- 새 데이터 소스를 붙이기 전에 `node -e "fetch(...)"`로 실제 응답을 확인하라
- 지표 모듈은 반드시 `lines` (한국어 문장 배열)를 함께 내라. 그게 프롬프트에 들어간다
- **복원 불완전한 규칙은 반드시 "추정"으로 표시한다.** `neowave.js`의 `estimated`/`partial`
- **다이아고날은 R3가 면제된다.** `elliott.js`의 `detectDiagonal()`이 수렴 형태를
  확인한 경우에만 R3 위반을 통과시킨다. **이 면제를 다른 규칙으로 넓히지 마라** —
  R1·R2는 다이아고날에서도 절대 규칙이다
- **닐리 관련 수치는 반드시 `neely-patterns.js` 를 통해 써라.** 각 값에 원서 쪽수가 달려 있다.
  `corrective.js` 는 플랫 8종·지그재그 3종 분류를 이 카탈로그에 위임하고,
  `elliott.js` 는 유사성·시간론·예외의 법칙을 여기서 가져다 쓴다
- **중재는 적중률이 아니라 `edge`(적중률 − 기저율)로 비교한다.** 적중률만 보면
  기법이 실제보다 좋아 보인다 — 초기 구현에서 닐리 국면 판정이 84%로 나왔는데
  검증식이 분류식과 같은 조건을 다시 묻는 자기상관이었다. **새 백테스트를 추가할 때는
  반드시 기저율을 함께 내고, 검증량이 분류량과 다른지 확인하라**
- **기법의 전제를 지켜서 백테스트하라.** 앵커 채널은 "임펄스일 때"의 기법이므로
  절대 규칙을 통과한 창에만 적용한다. 아무 데나 갖다 대고 안 맞는다고 하면
  기법을 부당하게 깎는 것이다. 표본이 5건 미만이면 우열을 말하지 마라
- **피보나치 수열 매물대는 비트 전용이다.** 원문 근거가 비트 사례뿐이라
  `fibSequenceLevels` 가 심볼을 검사해 BTC 외에는 null 을 낸다. 이 검사를 풀지 마라
- **VIP방 대화(`ChatExport …(5)`, 1.6MB)는 아직 규칙화하지 않았다.** 그쪽은
  피보 타점의 **사례 모음**이지 규칙이 아니다. 별도 분석 없이 끌어다 쓰지 마라
- **로그 척도는 "고르는" 게 아니라 "갈리는지 보는" 도구다.** 등락폭 2배 이상 구간에서
  로그가 주 척도가 되지만, 다른 척도에서만 나오는 법칙도 후보로 남긴다. 조건 문자도
  같은 척도로 재라 — 법칙은 로그로, 조건은 산술로 재면 앞뒤가 어긋난다
- **`preconstruction.js`는 32/32 조건이 전부 채워져 있다** (법칙 1~7). 다만 조건마다
  원문의 가장 명확한 관문 1~3개만 옮긴 것이지, 문단 안의 모든 중첩 조건을 옮긴 게
  아니다 — 이 밀도는 의도적이다(법칙 1·2 때부터의 원칙). 표에 없는 (법칙,조건) 쌍이
  다시 생기면(예: 새 법칙 추가) `COVERAGE`가 "미수록"으로 보고하니 감추지 마라.
  항목을 추가하거나 고칠 때는 반드시 **원문을 직접 읽고** 조건 문자와 쪽수를 함께 박아라
- **복합조정의 결론은 x파동 크기 하나로 갈린다.** x/W가 61.8% 이하면 범주1(세력 +2),
  161.8% 이상이면 범주2(세력 −3, 러닝이면 +3)다. **그 사이는 닐리도 정의하지 않았으므로
  한쪽으로 확정하지 말고 양쪽 세력을 모두 내라** (`powerAlt`). 러닝 판별을 빠뜨리면
  같은 범주 안에서 세력이 −3과 +3으로 뒤집힌다
- **예외의 법칙이 하드 리밋이다.** 중요 원칙이 2개 이상 동시에 깨지면 그 카운트는 폐기한다.
  하나만 깨지는 것은 삼각형·터미널에서 허용되므로 폐기하지 마라
- 미구현 목록은 `docs/파동분석-통합본.md` 8장에, **자료 간 충돌과 그 결정**은 9장에 있다
- **하모닉 XABCD 앵커링을 완화하지 마라.** 확정 피벗·ATR 최소 크기·봉 간격 5종 필터가
  없으면 하모닉은 어느 차트에서든 "발견"되어 근거가 아니라 소음이 된다
- **근거 중첩은 같은 출처를 하나로 친다.** 이동평균 5개가 겹쳐도 근거는 하나다.
  클러스터 폭 상한(ATR×1.0)도 풀지 마라 — 연쇄 병합으로 구간이 무한정 넓어진다
